package com.week.nine.day.one;

public class Interface {
   
	//Only method signatures are allowed in interfaces
	//All interfaces have keyword interface
	//To use interface with class you use keyword "implements"
	//It has behaviors for method signatures only
	//Can not code in interface
	//All of the method does NOT have a body
	
	
	//You can implement multiple interface to one class separated by comma; extends does not allow this
	//Hover over word and "Add unimplemented methods" in class
	//Interface is another form of class
	//All the methods in interfaces should implement in class that using the interfaces (otherwise you get a compile error)
	//ONLY method signatures allowed in interfaces
	//Does not support multiple inheritance
	//Can NOt instantiate from interface
	//private access modifier not allowed
}
